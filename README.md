1.inside path ./iTNotes run command :- npm init

2.again inside path ./iTNotes/frontend run command :- npm init

    these above two commands will create two node_modules folder one inside root directory and one inside frontend folder
		
3.to run application , open two command prompt for runnning the backend on one and frontend on another
   a.for running frontend , inside path ./iTnotes/frontend run command :- npm start
   b.for running backend , inside path ./iTnotes run command :- npm run dev
